<h1>TODO: List all students with COURSES</h1>
<ul>
    {{#each students}}
        <li>{{this.first_name}}</li>
        <li>{{this.last_name}}</li>
        <li>{{this.hero_name}}</li>
        <li>{{this.power}}</li>
    {{/each}}
</ul>